var errors = $('<div class="alert alert-error"/>');

<% @micropost.errors.full_messages.each do |error| %>
  errors.append('<p><%= j(error) %></p>');
<% end %>

// Display errors before the form
$('#new_micropost').prepend(errors);
errors.fadeOut(3000);